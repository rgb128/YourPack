'use strict';

const canvas = document.getElementById('canvas');
const ctx = canvas.getContext('2d');

// async function onUploading() {
//     console.log('uploading...');
// }
async function onUploaded(image) {
    // img base64 = image.src
    console.dir(image);
}
